# Database Configuration
POSTGRES_URL=jdbc:postgresql://localhost:5432/tech_interview_sim
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password



# JWT Configuration
JWT_SECRET=your_jwt_secret_key_at_least_256_bits_long
JWT_EXPIRATION=86400000

# Supabase Configuration (if using Supabase auth)
SUPABASE_PROJECT_URL=your_supabase_project_url
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Server Configuration
PORT=8080

# Other Settings
SPRING_PROFILES_ACTIVE=dev

# Logging Configuration
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_KOUSHIK=DEBUG



# Note: Copy this file to .env and fill in your actual values
# Do not commit the actual .env file to version control
